apiVersion: v1
kind: Service
metadata:
  name: sample_app_fe_service
  namespace: argocd
  labels:
    app: sample_app_fe
spec:
  type: LoadBalancer
  selector:
    app: sample_app_fe
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
